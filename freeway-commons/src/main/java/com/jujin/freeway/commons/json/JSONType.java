package com.jujin.freeway.commons.json;

/**
 * Represents the different data types supported by JSON. Mostly used for
 * descriptive reasons in exceptions.
 */
public enum JSONType {

    BOOLEAN, STRING, NUMBER, OBJECT, ARRAY, ANY;
}
